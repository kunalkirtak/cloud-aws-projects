[pytest]
testpaths = tests
pythonpath = .
addopts = -q
markers =
    integration: requires a real PostgreSQL + pgvector database (TEST_DATABASE_URL)
    slow: downloads/loads the real embedding model (RUN_MODEL_TESTS=1)
